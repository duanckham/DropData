# DropData

How to use
----------

```
const DropData = require('dropdata');

const dd = new DropData({
  accessToken: '***',
  project: 'ac',
  path: 'storage',
  name: 'ac',
  // debug: false,
});

// So you will get the lowdb object: dd.db
```
